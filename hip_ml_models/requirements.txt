# Requirements for hip_ml_models
# Note: torch with CUDA (e.g. +cu118) often requires installing from the PyTorch index.
# Install torch separately using the PyTorch instructions for your platform.

torch==2.7.1
torchaudio==2.7.1
torchvision==0.22.1
opencv-python==4.12.0.88
numpy==2.2.6
Pillow==11.3.0
matplotlib==3.10.6
